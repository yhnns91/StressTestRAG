name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest -q

      - name: Validate corpus metadata
        run: |
          shopt -s nullglob
          for f in corpus/metadata/*.csv; do
            echo "Validating $f"
            python scripts/validate_metadata.py "$f"
          done
        shell: bash
